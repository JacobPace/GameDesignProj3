using UnityEngine;

public class AudioSystemRoot : MonoBehaviour
{
    public static AudioSystemRoot Instance;

    void Awake()
    {
        if (Instance != null)
        {
            Destroy(gameObject);
            return;
        }

        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
}